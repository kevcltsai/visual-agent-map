# Visual Agent Map 產品核心

版本：v0.4.0  
建立日期：2026-09-12  
文件定位：產品與程式建置的唯一規格來源。實作、測試及其他文件若與本文件衝突，以本文件為準。

## 產品目標

Visual Agent Map 是一個 Obsidian 外掛，以心智圖組織複雜問題、以 Markdown 保存知識，並以獨立的 AI 任務推進思考。

使用者應能從一張圖快速回答：

1. 這個議題被拆成哪些部分？
2. 每個部分目前理解到哪裡，下一步是什麼？
3. 哪些子議題結果值得整合回母議題？

圖上只顯示足以判斷方向的摘要；完整內容保存在可直接閱讀及編輯的 Markdown 筆記。產品不把節點描述成具有永久記憶的 Agent；每次 AI 執行都是獨立任務，所需脈絡必須從目前資料重新組裝。

## 資料模型與權威來源

### 心智圖

每個研究主題是一個獨立資料夾，固定包含一份主要心智圖及其議題筆記：

```text
Agent Workspace/
├── Topics/
│   └── 主題名稱/
│       ├── Map.md
│       ├── Notes/
│       ├── Unassigned/
│       └── Archive/
└── Inbox/
```

`Map.md` 是該圖結構的唯一權威來源，保存：

- 心智圖名稱與 ID。
- 節點筆記引用。
- 節點的單一母子關係。
- 節點座標與收合狀態。
- 畫面視角與縮放比例。

結構必須保持無循環。節點可以更換母議題或解除連結。

### 議題筆記

每個節點對應一份 Markdown 筆記。一份議題筆記同一時間只能屬於一張心智圖，避免不同心智圖的結構與脈絡互相影響。

議題筆記包含：

| 欄位 | 用途 |
|---|---|
| 議題 | 節點正在處理的問題或主題 |
| 目前理解 | 現階段最重要的資訊、暫定判斷與研究進度 |
| 下一步任務 | 下一次要請 AI 完成的具體工作 |
| Detail | 去重後的累積知識頁，固定包含核心結論、關鍵知識、證據與來源、取捨與限制、待確認事項及精簡更新紀錄；必要的圖片與視覺參考也自然保存在這裡 |
| User Notes | 使用者自行撰寫、AI 整合不得覆蓋的內容 |
| Model | 此節點執行 AI 任務時使用的模型 |
| Model 來源 | 工作區預設、建立時繼承或手動指定 |
| 狀態 | 草稿、AI 執行中、已完成或錯誤 |
| 所屬心智圖 ID | 確保筆記只屬於一張心智圖 |
| 所屬主題 ID | 筆記在 Notes、Unassigned、Archive 之間移動時仍保留的穩定主題識別 |
| 主題狀態 | active、unassigned、archived 或 inbox |
| 來源議題 | 整合議題使用的穩定來源筆記路徑，可在工作台點回原議題 |

「目前理解」不是最終結論，可以由使用者編輯或由 AI 任務更新。

議題筆記在 Obsidian 中預設隱藏 Properties 區塊，讓閱讀焦點留在議題內容；frontmatter 仍完整保存在 Markdown 檔案中。

### Obsidian reference

議題筆記中的 `[[wikilink]]` 是方便瀏覽的衍生資料，不能反過來決定圖面結構。Reference 至少列出所屬心智圖、母議題與直屬子議題。

Reference 必須遵守：

- 只修改外掛管理的 frontmatter metadata。
- 每次重新產生完整 metadata，不重複追加。
- 保留 frontmatter 中非外掛欄位及所有非受管理段落。
- 節點、筆記或心智圖重新命名後更新連結。
- Reference 錯誤時，以心智圖檔案重新建立。
- 單純開啟心智圖不得改寫議題筆記。

## 建立與開啟心智圖

- 使用者可以新增、開啟、重新命名及刪除研究主題。新增時建立主題資料夾、`Map.md`、`Notes`、`Unassigned` 與 `Archive`。
- 重新命名主題時同步更新資料夾名稱及 Map 標題；`topic-id` 不變。
- 刪除心智圖時只將 `Map.md` 移至 Vault 垃圾桶，保留整個主題資料夾與筆記。主題進入「Map 遺失」狀態；筆記不會因此自動開放給其他主題認領。
- 在 Obsidian 開啟 Map Markdown 時，自動在該分頁轉成 Visual Agent Map 畫面。
- 開啟心智圖是讀取操作，不得觸發內容或 reference 寫入。

## 建立與加入議題

- 可以建立根議題或在既有節點下建立子議題。
- 可以將尚未屬於任何心智圖的既有議題筆記加入目前心智圖。
- 已屬於其他心智圖的筆記不得直接加入；介面應說明原因。
- 可以進入「整合議題」模式，多選任意同層或跨分支議題；底部選取列顯示數量與名稱，下一步確認新議題名稱、整合目標及 AI 規則。
- 整合時讀取來源的目前理解與完整 Detail，保留來源議題原狀，AI 完成後建立新的根議題；來源以穩定路徑保存，並可在工作台點回原議題。
- 認領目前主題的未歸類筆記時，筆記移回 `Notes` 並先成為根議題；使用者之後可以指定母議題。
- 從圖中移除節點時，Markdown 筆記移至目前主題的 `Unassigned`，保留 `topic-id` 並清除 `agent-map-id`。
- 移除單一節點時，該筆記移至 `Unassigned`，直屬子議題留在 `Notes` 並成為根議題。
- 移除整個分支時，分支內所有筆記移至 `Unassigned`；不永久刪除筆記。

## 未歸類、封存與跨主題移動

- `Unassigned` 保存仍屬於目前研究主題、但暫時不在圖上的筆記；可重新認領為根節點。
- 只有 `Unassigned` 筆記可以移入 `Archive`。封存筆記不出現在一般認領清單，也不提供給 AI 作為背景。
- Archive 筆記必須先取消封存並回到 `Unassigned`，才能重新加入心智圖。
- 筆記可「移至其他主題」並進入對方 `Unassigned`，或「移動並加入心智圖」直接進入對方 `Notes` 成為根節點。
- `Inbox` 保存無法判斷原始主題的舊孤兒筆記。使用者必須明確指定主題，程式不得依檔名或內容猜測歸屬。
- 活動筆記的 reference 列出主題、Map、母議題與子議題；未歸類或封存筆記只保留主題及狀態；Inbox 不保留主題 reference。

## 父子議題脈絡

父子關係同時代表圖面結構與 AI 背景傳遞方向。

### 母議題向下提供背景

新子議題建立時，複製母議題當下的 Model 與 AI 規則。建立後可獨立修改，不持續同步。

子議題每次執行 AI 任務時，自動取得所有祖先節點最新的：

- 議題。
- 目前理解。
- AI 規則。

母議題更新後，不自動覆寫既有子議題內容或規則。

### 子議題向上形成整體理解

母議題可執行「整合子議題發現」，集中讀取直屬子議題的目前理解與完整 Detail。使用者在執行前確認任務；AI 完成後直接以新摘要與固定格式知識頁取代舊內容。

新的子議題整合結果使用與一般 AI 任務相同的六段知識格式。`User Notes`、Model、父子關係、Reference 與所有子議題筆記不得被整合覆蓋。

子議題結果不得自動覆寫母議題。

## AI 任務

### 任務輸入

- 研究、比較、風險檢查、「自己描述下一步」與執行已保存任務使用同一套直接寫回流程：執行前確認一次，完成後直接更新目前理解與 Detail。
- Decompose 用於快速產生子議題建議，應使用低成本、低 reasoning 與最小 context；完成後不得自動跳窗，只顯示待查看入口。
- Synthesize 用於高品質整合直屬子議題發現，應使用較高 reasoning；執行前確認一次，完成後直接寫回。

每次 AI 任務必須包含：

1. 目前節點的議題。
2. 目前節點的目前理解。
3. 目前節點的完整 Detail，要求整合而非追加。
4. 目前節點的 AI 規則，並要求模型優先遵守。
5. 目前節點的下一步任務。
6. 所有祖先節點的議題、目前理解與 AI 規則。
7. 若有舊版 Working Findings 或整合來源，必須一併帶入並收斂。

不同節點的任務可以平行執行。

### 任務輸出與寫回

任務輸出包含：

- 一句適合顯示在節點上的新「目前理解」。
- 完整 Markdown 知識頁，依序使用「核心結論、關鍵知識、證據與來源、取捨與限制、待確認事項、更新紀錄」六個三級標題。
- 必要時提供圖片、色票或其他視覺參考；寫回時必須併入 Detail，不建立獨立 Visual References 區塊。
- 必要時提供拆解建議，但不直接改動圖面結構。

AI 成功後的寫回規則：

- 立即以新的摘要取代「目前理解」，並以去重、整合後的完整知識頁取代 Detail；不得逐次堆疊完整回答。
- 更新紀錄每次只增加一行變更摘要；若筆記含舊版 Working Findings，必須吸收到合適段落後再移除舊區塊。
- 將狀態改為「已完成」。
- 不自動建立、刪除、合併或重新連接節點。

AI 失敗時：

- 將節點狀態改為「錯誤」。
- 在介面顯示可理解的錯誤訊息。
- 不修改目前理解、Detail 或其他議題內容。
- 技術錯誤只寫入外掛紀錄，不寫入議題筆記正文。

## AI 拆解議題

使用者可以要求 AI 將過於複雜的議題拆成 3–7 個可獨立處理、合起來能回答母議題的子議題提案。拆解任務屬於 Decompose，只產生提案，不使用一般內容寫回流程。

每項提案包含：

- 子議題名稱。
- 要回答的問題或任務。
- 對母議題的貢獻。

使用者可以勾選、修改或刪除提案；確認後才建立子節點。需要提案以外的子議題時，可在確認後使用空白子議題入口補充。一般 AI 回應若判斷議題過於複雜，將提案暫存並顯示「查看 AI 子議題建議」入口，不得自動彈出提案視窗或建立節點。只有使用者主動點擊該入口，或明確選擇「拆成子議題」時，才開啟提案視窗。

建立空白子議題與 AI 拆解集中在「下一步」及右鍵選單。Decompose 完成後不自動彈窗，只在節點上顯示查看建議入口。

## Model 規則

- 工作區預設使用目前可用的最低成本模型；目前指定為 `gpt-5.6-luna`。
- 一般任務使用 `low` reasoning；Synthesize 使用 `high` reasoning。
- Model 可使用 provider 前綴；`gpt-*` 預設走 `codex-acp` 常駐 session，`codex exec` 僅作 Advanced fallback；`claude:sonnet`、`claude:opus`、`claude:fable` 走 Claude Code CLI。
- 根議題建立時使用工作區預設 Model。
- 子議題建立時複製母議題當下的 Model。
- 節點建立後可以獨立變更 Model。
- 節點必須顯示實際模型與來源：工作區預設、建立時繼承或手動指定。
- 若無穩定介面取得 Codex 或 Claude Code 帳號可用模型，由使用者維護候選清單，並允許輸入自訂模型 ID。
- 外掛不保存 API key。

## 節點與圖面互動

- 圖面顯示議題、目前理解與需要注意的狀態；成功狀態保存在資料中，但不顯示常駐徽章。
- 點選節點後，在工作台編輯議題與目前理解；Model 與連結收在進階設定。
- 節點右上角提供詳情按鈕。
- 第一次開啟詳情時建立一個專用的右側 Markdown 欄；之後切換節點時重用同一欄，不持續新增分頁。
- 節點可以拖曳、收合或展開分支；畫布可以平移、縮放及顯示全部。
- 節點可更換母議題、解除連結、從圖中移除或移除整個分支；任何操作不得建立循環。
- 右鍵選單與設定區必須呼叫相同的底層操作，不得形成不同規則；卡片只保留詳情與分支收合，不重複放新增或 AI 拆解捷徑。
- 結構修改支援復原與重做。AI 結果或外部檔案更新不可被舊結構快照覆寫。
- 復原與重做涵蓋節點、連結、認領、檔案搬移、封存及 AI 整合覆蓋，保存在目前 Obsidian 工作階段，不跨外掛重載。
- 若相關檔案已被外部修改、移動或刪除，停止不安全的復原或重做並提示使用者。

## 多選與整合

- 使用者由工具列進入明確的整合模式，以一般點擊勾選同層或跨分支議題；底部選取列提供清除與下一步。
- 下一步顯示來源名稱，並讓使用者確認新議題名稱、整合目標與本次 AI 規則。
- AI 讀取每個來源的完整累積知識，去重後以六段固定格式建立新的根議題；AI 失敗時不得建立空殼節點。
- 新根議題保留穩定的來源路徑，工作台將來源顯示為可點擊按鈕；舊版短 wikilink 也必須能解析。
- 建立整合議題時保留原節點、原筆記與原有子節點。

## 外部 Markdown 同步與衝突

- 外部修改 Map 或議題 Markdown 後，畫面必須更新。
- 使用者正在輸入時，延後套用外部修改。
- 欄位在畫面與外部 Markdown 都被修改時，必須提示衝突，讓使用者選擇保留畫面內容、使用檔案內容或手動合併。
- 不得以最後寫入者直接覆蓋另一方內容。
- 檔案被重新命名時，更新心智圖中的節點路徑及相關 reference。
- 檔案遺失時，節點仍顯示在圖上並提供重新連結或從圖中移除的操作。

## 資料安全要求

- Map 與議題筆記維持可直接閱讀的 Markdown 格式。
- 外掛只修改自己管理的 frontmatter 欄位與明確標記的受管理段落。
- Map 結構寫入成功後即為權威狀態；reference 寫入失敗不得回滾或偽裝成 Map 寫入失敗。
- Reference 是可重新建立的衍生資料，必須提供重建方式。
- AI 的 Codex provider 預設使用 `codex-acp` 常駐 session；fallback 的 `codex exec` 必須使用 read-only sandbox。
- AI 結果寫回、資料遷移與批次 reference 更新必須經過資料保存測試。
- 舊版全域 Maps／Nodes 只能透過使用者主動開啟的遷移預覽整理；確認前不得移動檔案。遷移須顯示主題、圖內筆記及孤兒數量，並在失敗時還原所有已搬移檔案。
- `Map.md` 遺失時保留整個主題資料夾，提示重新連結既有 Map，或由 Notes 重建所有節點皆為根節點的空白 Map。

## 驗收條件

程式至少必須驗證：

1. 每次的部署需要自己產生測試驗證, 最終驗收必須在 Obsidian 上操作成功。
